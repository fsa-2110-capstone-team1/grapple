import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#D8F1F6",
      main: "#248DA2",
      dark: "#196170",
      contrastText: "#fff",
    },
    secondary: {
      light: "#E0F6FE",
      main: "#0788B7",
      dark: "#022B3A",
      contrastText: "#fff",
    },
  },
});

export default theme;
