import React from 'react';

export const UserDashboard = () => {
  return (
    <h3>
      This page will house the dashboard for the user and potentially the main
      hub for when a user logs in.
    </h3>
  );
};
export default UserDashboard;
